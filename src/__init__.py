"""Audio Manager — gestionnaire audio PipeWire/WirePlumber pour Linux.

Ce fichier est la SEULE source de vérité pour le numéro de version.
Pour publier une nouvelle version, il suffit de changer `__version__` ici :
tout le reste de l'application (fenêtre principale, build PyInstaller,
user-agent HTTP, etc.) le lit depuis `src.config`.
"""

__version__ = "0.0.1"
